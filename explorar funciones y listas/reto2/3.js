// Crear una función que reciba un número como parámetro y devuelva el doble de ese número.

let numero = 5;

function recibir(numero){
    return numero * 2;
}

 console.log(recibir(numero)); //a la hora de imprimir también toca llamar el parametro